@model IEnumerable<DoctorSkin.Models.Wishlists>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.iduser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idp)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.iduser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idp)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.stt_wl }) |
                @Html.ActionLink("Details", "Details", new { id=item.stt_wl }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.stt_wl })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
